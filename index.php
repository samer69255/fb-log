<?

if ($_POST['email'])
{
/*
    $jsonString = file_get_contents('login.json') or "[]";
    $data = json_decode($jsonString, true);
    array_push($data,json_encode($_POST));

    $newJsonString = json_encode($data);
    file_put_contents('login.json', $newJsonString);

    echo 'saved';
    */
}

else {
     echo "hi";
}

?>